# 🚀 Meus Primeiros Passos em Python

Este repositório foi criado para documentar minha jornada inicial na programação com Python. Aqui estão os 5 projetos fundamentais que desenvolvi para consolidar conceitos de lógica, estruturas de dados e automação básica.

O objetivo deste portfólio é demonstrar minha evolução, desde os primeiros comandos de saída até a criação de funções e uso de bibliotecas externas.

---

## 📂 Projetos Iniciais

### 1. 🧮 Operadores e Variáveis (`calculadora.py`)
O início de tudo. Um script focado na manipulação de entradas do usuário (`input`), conversão de tipos de dados (`int`/`float`) e operações matemáticas básicas.

### 2. 🚦 Controle de Fluxo (`verificador.py`)
Aplicação prática de estruturas condicionais (`if`, `elif`, `else`). O projeto foca em como o programa pode tomar decisões baseadas em regras de negócio simples.

### 3. 🔄 Laços de Repetição (`tabuada.py`)
Uso de loops (`for` e `while`) para automação de tarefas repetitivas. Aqui aprendi a controlar o fluxo de execução e trabalhar com contadores.

### 🏫 Portal do Aluno (`dicionario.py`)
Sistema de gerenciamento de notas que utiliza **Dicionários** para associar nomes de alunos a notas.
* **Evolução Técnica:** Implementação de **Tratamento de Exceções (`try/except`)**. O sistema agora valida se a nota digitada é um número decimal válido, impedindo que o programa quebre caso o usuário digite texto por engano.
* **Lógica:** Uso de `while True` para criar um "loop de insistência", garantindo que o dado seja inserido corretamente antes de avançar.

### 🔐 Gerador de Senhas Aleatórias (`gerador_senhas.py`)
Um utilitário para criação de senhas seguras com caracteres variados.
* **Evolução Técnica:** Blindagem contra erros de entrada. Utilizei o bloco `try/except` para capturar falhas na definição do tamanho da senha.
* **Conceitos:** Uso das bibliotecas `random` e `string`, além de loops de repetição para construção da string final.

---

## 🛠️ Tecnologias Utilizadas
* **Linguagem:** Python 3.x
* **Bibliotecas Nativas:** `random`, `string`

## 📈 Evolução
Este repositório marca o início da minha transição para a área de tecnologia. Atualmente, estou focado em aprofundar conhecimentos em **Lógica de Programação** e me preparando para explorar **Análise de Dados**.

## 🛠️ Log de Evolução
"v1.0: Lógica básica e listas."

"v1.1: Adicionado tratamento de erros e loop de persistência."

"v1.2: Implementação de salvamento em arquivo .txt."

---
*Documentação estruturada com auxílio de IA e revisada por mim, Guilherme de Andrade*
